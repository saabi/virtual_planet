{
	"name": "@virtual-planet/graph-editor",
	"version": "0.0.0",
	"private": true,
	"description": "Reusable Svelte graph-editor components over the Typed Graph IR (no renderer logic).",
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"check": "tsc --noEmit",
		"test": "vitest run"
	},
	"peerDependencies": {
		"svelte": "^5.0.0"
	},
	"devDependencies": {
		"typescript": "^6.0.3",
		"vitest": "^4.1.9"
	}
}
