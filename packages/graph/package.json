{
	"name": "@virtual-planet/graph",
	"version": "0.0.0",
	"private": true,
	"description": "Typed Graph IR: nodes, ports (data + coordinate-space), validation, serialization.",
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"check": "tsc --noEmit",
		"test": "vitest run"
	},
	"devDependencies": {
		"typescript": "^6.0.3",
		"vitest": "^4.1.9"
	}
}
