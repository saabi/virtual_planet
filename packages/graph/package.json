{
	"name": "@world-lab/graph",
	"version": "0.0.0",
	"private": true,
	"license": "MIT",
	"description": "Typed Graph IR: nodes, ports (data + coordinate-space), validation, serialization.",
	"repository": {
		"type": "git",
		"url": "https://github.com/saabi/world-lab.git",
		"directory": "packages/graph"
	},
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"check": "tsc --noEmit",
		"test": "vitest run"
	},
	"dependencies": {
		"@world-lab/schema": "*"
	},
	"devDependencies": {
		"typescript": "^6.0.3",
		"vitest": "^4.1.9"
	}
}
