{
	"name": "@virtual-planet/runtime-webgpu",
	"version": "0.0.0",
	"private": true,
	"description": "WebGPU runtime: buffers, pipelines, bind groups, consumers, tessellation scheduling.",
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"check": "tsc --noEmit",
		"test": "vitest run"
	},
	"devDependencies": {
		"typescript": "^6.0.3",
		"vitest": "^4.1.9"
	}
}
