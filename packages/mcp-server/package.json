{
	"name": "@world-lab/mcp-server",
	"version": "0.0.0",
	"private": true,
	"license": "MIT",
	"description": "MCP server exposing the Typed Graph IR to AI assistants (documents, sessions, compile, diagnostics).",
	"repository": {
		"type": "git",
		"url": "https://github.com/saabi/world-lab.git",
		"directory": "packages/mcp-server"
	},
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"check": "tsc --noEmit",
		"test": "vitest run"
	},
	"dependencies": {
		"@world-lab/graph": "*",
		"@world-lab/schema": "*"
	},
	"devDependencies": {
		"typescript": "^6.0.3",
		"vitest": "^4.1.9"
	}
}
