{
	"name": "@virtual-planet/mcp-server",
	"version": "0.0.0",
	"private": true,
	"description": "MCP server exposing the Typed Graph IR to AI assistants (documents, sessions, compile, diagnostics).",
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"check": "tsc --noEmit",
		"test": "vitest run"
	},
	"devDependencies": {
		"typescript": "^6.0.3",
		"vitest": "^4.1.9"
	}
}
